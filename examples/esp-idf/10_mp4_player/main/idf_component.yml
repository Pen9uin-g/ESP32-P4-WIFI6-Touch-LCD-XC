dependencies:
  idf: ">=5.4"

  waveshare/esp32_p4_wifi6_touch_lcd_xc:
    version: "2.0.0"

  espressif/esp_audio_codec:
    # v2.6+ requires ESP32-P4 rev >= 3.0; this board's default configuration
    # targets the supported rev 1 hardware, so keep the last compatible line.
    version: ">=2.3.0,<2.6.0"
    public: true
