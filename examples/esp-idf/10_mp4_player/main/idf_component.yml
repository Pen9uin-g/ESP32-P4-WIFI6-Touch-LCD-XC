dependencies:
  idf: ">=5.4"

  waveshare/esp32_p4_wifi6_touch_lcd_xc:
    version: "3.0.1"

  espressif/esp_audio_codec:
    # v2.6+ requires ESP32-P4 rev >= 3.0. Keep the published 2.5.0 release so
    # the explicit rev1_3 compatibility profile remains buildable.
    version: "2.5.0"
    public: true
